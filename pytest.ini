[pytest]
testpaths = test
timeout = 60
markers =
    unittest: deterministic unit tests that require no external network
    windows_native: native Windows filesystem and hard-exit integration evidence
    e2e: data-plane cases that need the JERRYPROXY_E2E_* environment; they skip without it
